package com.nva.RealTimeMessenger_v22.util;

import java.util.Base64;

public class TokenHelper {
    public static Base64.Encoder encoder = Base64.getEncoder();

    public static Base64.Decoder decoder = Base64.getDecoder();

}
